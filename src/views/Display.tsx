import React from "react";
//maybe set boolState in Views.tsx when navlink hits to change condition for (displaying homepage or displayRoutes in Views.tsx)
//make sure data from routesInstance displays properly
//css Display.tsx
//video, after effects, and code editor stuff

const Display = (data: any) => {
  return (
    <div>
      <h1>{data.Title}</h1>
      <h1>
        data.Titledata.Titledata.Titledata.Titledata.Titledata.Titledata.Titledata.Titledata.Title
      </h1>
    </div>
  );
};

export default Display;
